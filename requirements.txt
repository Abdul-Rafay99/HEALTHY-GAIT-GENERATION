# torch is intentionally not pinned here - it's hardware/CUDA-specific.
# Install it separately, matching the target machine's GPU and CUDA version
# (see https://pytorch.org/get-started/locally/), before installing the rest
# of this file. On a machine that already has a working CUDA install (e.g.
# one that's trained successfully before), reuse that environment rather
# than letting this file's pins touch it.
contourpy==1.3.0
cycler==0.12.1
filelock==3.19.1
fonttools==4.60.2
fsspec==2025.10.0
importlib_resources==6.5.2
Jinja2==3.1.6
kiwisolver==1.4.7
MarkupSafe==3.0.3
matplotlib==3.9.4
mpmath==1.3.0
networkx==3.2.1
numpy==2.0.2
packaging==26.3
pandas==2.3.3
pillow==11.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
pytz==2026.3.post1
scipy==1.13.1
six==1.17.0
sympy==1.14.0
tqdm==4.70.0
typing_extensions==4.16.0
tzdata==2026.3
zipp==3.23.1
